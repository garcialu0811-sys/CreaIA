-- obtener el valor menor del campo de edad de la tabla users
SELECT MIN(age) AS min_age
FROM users;

-- obtener el valor mayor del campo de edad de la tabla users
SELECT MAX(age) AS max_age
FROM users;