-- nos va a generar el reusltado con los datos no duplicados

-- todos los nombres de los usuarios sin duplicados
SELECT DISTINCT name FROM users;

-- todos los datos distintos entre si de la tabla users
SELECT DISTINCT * FROM users;